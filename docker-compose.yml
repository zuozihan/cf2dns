services:
  cf2dns:
    build:
      context: .
      dockerfile: Dockerfile
    image: zuozihan/cf2dns:latest
    container_name: cf2dns
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
      - LOG_FILE=/app/logs/cf2dns.log
      - INTERVAL_SECONDS=${INTERVAL_SECONDS:-900}
    volumes:
      - cf2dns_logs:/app/logs

volumes:
  cf2dns_logs:
